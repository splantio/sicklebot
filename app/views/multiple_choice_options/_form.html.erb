<%= form_with(model: multiple_choice_option, local: true) do |form| %>
  <% if multiple_choice_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(multiple_choice_option.errors.count, "error") %> prohibited this multiple_choice_option from being saved:</h2>

      <ul>
        <% multiple_choice_option.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.number_field :question_id %>
  </div>

  <div class="field">
    <%= form.label :option %>
    <%= form.text_field :option %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
